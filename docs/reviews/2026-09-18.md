# Design-Docs Sweep — 2026-09-18 (nightly)

Window reviewed: Discord gateway log through 2026-09-17 18:54 (no 2026-09-18
messages yet); free-response channel 1544705031742423131 silent in the window.
Design statements recovered via session history (three threads from 2026-09-17).

## Captured this pass (3)

- **PC-DEC-045** (Spahrep): "is Player_max_HP in a config talbe, it shouldnt' be hard
  coded." → player max HP config-driven via `game_config.starting_hp`. Shipped
  271a75d (2026-09-17), never captured until now. DECIDED.
- **PC-DEC-046** (DarkJester): same-enemy double-tap — when the first attack kills,
  the other hand starts its own move's cooldown immediately instead of being
  redirected or missing. Shipped 48ddd58, never captured until now. DECIDED.
- **PC-DEC-047** (DarkJester): attack damage multipliers get base ± range with an
  even spread (Heavy Chop x1.5 → 1.3–1.7 example). Shipped on branch wt/pc-65
  (kanban done), never captured until now. DECIDED.

Applied to: combat-system.md, current-design-status.md, battle-status-ui.md,
potion-contract.md, run-ux-flow.md.

## Conflicts / stale items flagged

1. **F1 — encounter-system.md internal contradiction (still open):** § Monster
   Selection states the 5th-monster rule as decided — "**5th monster**: Takes the
   largest possible remaining value (absorbs remaining points)" (L84) — while
   § Open Questions #2 still asks "does it pick the closest-cost monster from the
   mapping, or does it 'upgrade' a template to match the remaining budget exactly?"
   (L98). Same item both asserted and open. Kanban PC-58 (blocked) confirms the
   human ruling is still pending; L84 should be treated as pending until then.
2. **F2 — current-design-status.md header stale:** "as of 2026-09-16" while content
   includes 2026-09-17 decisions (PC-DEC-032..044). Header updated to 2026-09-18
   in this pass.
3. **F3 — PC-DEC-046 partially resolves "death-cancels-in-flight = PMVP":**
   current-design-status.md L69 + battle-status-ui.md § PMVP — Cancel on death both
   parked the whole mechanic as PMVP; the same-enemy double-tap case is now ruled
   (applied in this pass). Remainder — a monster dying on a tic still resolving its
   own in-flight attack — still PMVP.
4. **F4 — native-CLI parity text survived the PC-DEC-041 purge:** run-ux-flow.md L64
   ("Native CLI gets the same flow... byte-for-byte parity preserved") and
   current-design-status.md L149 ("Native CLI same flow... parity preserved")
   contradict PC-DEC-041 (native CLI deleted 2026-09-17). Fixed in this pass.
5. **F5 — potion-contract.md hardcoded HP cap:** L20 "capped at `PLAYER_MAX_HP`
   (1000, `js/combat/participants.js`)" vs PC-DEC-045 — the cap is now config-driven
   (`game_config.starting_hp`); 1000 is the offline engine default only. Fixed in
   this pass.
6. **F6 — admin-gui-brief.md attack fields predate PC-65 (flag only, brief is
   historical):** lists `base_damage_multiplier` as the attack field; PC-65 added
   `base_damage_multiplier_range` + band preview. No edit — briefs stay as written.

## Not captured (operational, no design delta)

- Heavy Chop double-count bug fix (31430f0) — bug fix; served as the gate before
  PC-DEC-047's ranges.
- Engine time-skip (c023c77, run-75 softlock fix) — implementation directive
  ("Is the code going through each tick one by one? That wastes computational power.
  It should look for when the next action is and then skip to that point while
  adjusting every other action by the amount skipped"), byte-identical behavior; no
  design content.
- "Create the ticket, and have the mult adjustment be done how ever you think is
  best" (16:50) — explicit delegation; noted inside PC-DEC-047's notes.

## Open items status

- **PC-DEC-004 OPEN** (timing preview/marker system revisit — still no ticket).
- **Blocked DECISION cards:** PC-57 (consumable pre/post + hand-free), PC-58
  (multi-enemy damage reduction + 5th-monster absorption), PC-59 (AP refund on
  completion), PC-60 (Player Journey section in core-philosophy.md).
  PC-56 / 61 / 62 resolved since the 2026-09-16 briefs.
- **Blocked non-decision cards:** PC-50r (stale REDO, superseded by PC-50r2 done),
  PC-37R2 (consumables v2 follow-up).
