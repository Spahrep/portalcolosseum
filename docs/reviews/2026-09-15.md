# Design Docs — Weekly Conflict Sweep — 2026-09-15

Sweep of `docs/pending-decisions.md` + all permanent docs against Discord thread history
(#the-forge game-design threads, 2026-09-15). Review only — no permanent doc was touched.
Per `pc-decision-capture` sweep rules, candidates are listed for human review, never auto-captured.

## 1. Conflicts between approved decisions and existing docs

### C1 — Monster instance purge (APPROVED 2026-09-15) vs schema notes in three docs
Spahrep approved purging the `monster_instance` table (kanban PC-53 done-record) —
quote: *"ok, let's purge the monster instnace then."* (16:36:43, preceded at 16:30:21 by
*"ok, so you are saying monster instnaces only exist in the JSON for the battle?"*).
Monsters now live only in `battle_state` JSON; `generate_monster()` no longer persists rows
(migration `20260915235000`, uniform HP roll).

Existing docs still describe the old schema:
- `current-design-status.md` L93: *"**SCHEMA (2026-09-10):** `monster_template.base_hp` + `monster_template.max_hp_delta`; `monster_instance.max_hp` (rolled). `generate_monster()` still rolls HP via Box-Muller `normal_int()` — MUST be switched to a uniform roll before the engine uses it."* — **double-stale**: the table is gone AND the uniform roll is already done.
- `combat-engine-plan.md` L9–13: *"Monster max HP is a **secret** rolled per instance (`monster_instance.max_hp`)… `generate_monster()` is `SECURITY DEFINER` — authenticated users cannot insert `monster_instance` rows directly… Supabase is the state of record: `portal_run` rows + `battle_state jsonb` + `monster_instance` rows."*
- `admin-gui-brief.md` L105–106 & L184–186 & L320: `monster_instance` in delete-block rules and in-scope references.

Verdict: approved decision wins; the three docs need updates at promotion time.

### C2 — Roulette "no spoiler" refinement vs the captured spec wording
Captured (encounter-system.md L44, portal-runs.md L50, Spahrep 2026-09-15): *"a highlight or
indicator moves across all the dice boxes quickly, then slows down, landing on the selected one."*
Later same day Spahrep rejected the reveal behavior — *"Ok, different but still a spoiler. the die
chosen was a red, and it was out of order, normally it goes green, yellow red, but this time a red
was 3rd and that's what it ended on… Are you capapble of just having it stay in the normal order and
randomly seelcting where it lands?"* (19:39:43). Shipped behavior (bcbf3e4): row always stays
G-block → Y-block → R-block; the sweep lands on a **random** box, uncorrelated with the drawn die;
the landed box morphs into the drawn die at the roll.

Verdict: the doc sentence "lands on the selected one" conflicts with the shipped/approved behavior.
Needs a capture that supersedes/qualifies it (see Candidate 5).

### C3 — Dragon-Warrior combat menu (uncaptured) vs battle-status-ui.md
`battle-status-ui.md` L148–149 (Decided 2026-09-08): *"**Bottom command boxes** = what I can do
right now (attack name + damage only; no bars, no timers — pure choices)"* and L26–64 Browse → Commit
flow: browsing shows a *"preview band in a lane just left of the queue column"* with cast-range /
hand-free-range labels.
Today Spahrep (19:49:05, 20:00:38 — see Candidate 4) specified a per-hand menu
(`Attack1...Attackn:C1:C2:BL`, no numbered rows), an attack → target → confirm flow, a consumable
confirm prompt, and *"when you select the attack, it shoudl show on the timing menu of upcomming
evnets, where it will slot in, could be a simple pair of ">" to show a range, or if it is fully
contained between 2 other timings, jsut one ">""* — a **simpler marker mechanism than the preview
band**.

Verdict: open question for Spahrep/DarkJester at approval — do the `>` markers **replace** the
preview band (L26–64), or coexist? The captured battle-status-ui mechanics are no longer fully locked.

## 2. Stale docs (header dates long past / superseded content)

| Doc | Header says | Actually contains |
|---|---|---|
| current-design-status.md | "as of 2026-09-08" | entries dated 2026-09-10/11/13/15; L93 schema note superseded (C1) |
| encounter-system.md | "Updated: 2026-09-07" | decisions dated 2026-09-11 and 2026-09-15 |
| portal-runs.md | "Updated: 2026-09-10" | animation spec dated 2026-09-15 |
| run-ux-flow.md | "Updated: 2026-09-13" | PC-50r section dated 2026-09-15 |
| battle-status-ui.md | "Decided (2026-09-08) — mechanics locked" | mechanics now being revised by the DW combat-menu direction (C3) |
| encounter-system.md Open Q #4 | "Face value calibration… (not the example 10/20/30)" | the 10/20/30 example became the actual prod face pools (2026-09-15); values are now DB-configured, only recalibration remains open |
| admin-gui-brief.md | (undated brief) | pre-purge `monster_instance` references (C1) |

Migration cluster (migration-plan-* + claude-migration-brief) is in-flight infra, not stale.

## 3. Duplicate / contradictory decisions

- **Roulette spec duplicated** in `encounter-system.md` L44 and `portal-runs.md` L50 — consistent
  with each other, but **both** carry the outdated "lands on the selected one" wording (C2).
  Benign duplication; one doc should stay authoritative (encounter-system.md).
- **battle-status-ui.md L172–174 open question** — *"Whether attacks are player-chosen (gui1 style)
  or proc on slot chance… unresolved. Current GUI = chosen."* — effectively **resolved today** by the
  DW menu spec (player-chosen, per-hand, from the granted attack set). Flag for closure at capture.
- combat-engine-plan.md L20 already lists monster **accuracy** as a rolled stat — consistent with
  the new player-accuracy decision (Candidate 2); no contradiction.

## 4. Uncaptured statements found in thread history (candidate PC-DEC entries)

Listed for human review — NOT auto-captured. Next free ID per pending-decisions.md numbering.

1. **Monster purge / monsters-in-JSON** (Spahrep, 2026-09-15 16:30/16:36) — C1 quotes above.
   → target: current-design-status.md + combat-engine-plan.md. (Approved via kanban PC-53; still no capture entry.)
2. **Player accuracy/misses** (Spahrep, 2026-09-15 20:34/20:36) —
   *"is our combat using accuracy? 70% accuracy means 70% chance to hit, otherwise its a miss, zero damage."*
   *"yes, please wire accuracy/misses into player attacks"*
   (shipped 7ef335d: player attacks roll weapon accuracy; miss = 0 damage; cooldown consumed).
   → target: combat-system.md (currently silent on accuracy) + weapon-generation.md
   (`base_accuracy`/`accuracy_range` now meaningfully applies to player attacks).
3. **AP + gold HUD dock** (Spahrep, 2026-09-15 19:42/20:10) —
   *"We need to add in AP and gold amounts on the GUI. I know we dont have these configured yet. we can
   hook up the back end later. give me a mockup showing what we ahve and where you think the AP and gold
   should go. While these are ok to know during combat, they are super important when in town."*
   *"I like the one that has the transparent bar on the bottom"*
   (shipped: bottom dock — name left, AP center, gold right; town/battle/equip pages; placeholder
   values; backend wiring later). → target: ap-economy.md.
4. **Dragon-Warrior combat GUI menu** (Spahrep, 2026-09-15 19:49/20:00) —
   *"we need to update the way the gui works for combat. I'm thinking more dragon warrior style. When it
   is your turn "LR or RH" you should get a menu with the attacks for that hand only. If you have both
   hands avail on the same tick, you do one and then the next, but always 1h at a time. your options on
   the menu are 1-n Attack options C1: C2: BL:"*
   *"There should be no numbered rows. Options are Attack1...Attackn:C1:C2:BL then after you hit
   AttackType you then chose the monster and confirm. If you use a consumeable it should just ask you to
   confirm you want to "Drink the healing potion for 10hp +(1-20) hp" Also when you select the attack, it
   shoudl show on the timing menu of upcomming evnets, where it will slot in, could be a simple pair of
   ">" to show a range, or if it is fully contained between 2 other timings, jsut one ">"."*
   → target: battle-status-ui.md (C3).
5. **Roulette normal order + random landing ("no spoiler")** (Spahrep, 2026-09-15 19:39) — C2 quote.
   → refines encounter-system.md L44 / portal-runs.md L50.
6. **Continue/stop prompt + loot selection later (still MVP)** (Spahrep, 2026-09-15 20:35) —
   *"After a battle of monsters is done, if there are more fights left, we need to have the user be
   prompted to Contine or stop. Then later (but still MVP) we need to give them the option on loot
   selection, we we havent determined. If they continue, we need to do another die select and then die
   roll exactly like we have done when you start a portal run."*
   (Verified in place in code — GUI `showAdvanceUI`, CLI continue/stop, die select+roll on continue.)
   → target: run-ux-flow.md; also narrows current-design-status.md Open Q #1 (loot selection at stop is
   confirmed, mechanism undetermined).

## 5. Surfaced open items (not decisions — no capture)

- **Schema-audit leftover** (Spahrep 2026-09-15 17:56: *"what is the next table/schema that isnt used
  adn isnt planned for use (if any)"*): audit confirmed no more orphan tables; remaining unused
  columns — `weapon_instance.slot_1..4_attack_id` + their 4 FKs (all NULL), `attack.weight`
  (mostly default 1.0). Awaiting Spahrep's call on dropping them. Not a decision yet.

## 6. Non-candidates reviewed and excluded

- 20:45:47 *"can you confirm that the numbers on the dice are from the template of the portal and not
  hard coded?"* — verification question; confirmed template-driven, consistent with docs.
- 20:42:28 belt-loop swap error + 20:46:31 handoff to Jester — bug report/workflow, not design.
- 16:29:30 mockup-attacks question — not a decision.
- 16:28:28 / 18:49:39 earlier roulette spoiler reports — subsumed by the 19:39 fix (Candidate 5).

## Notes

- No secrets/credentials appeared in the swept logs; nothing to redact.
- `pending-decisions.md` was read; no new entries were added this sweep (per rules, candidates
  require human approval before capture).
