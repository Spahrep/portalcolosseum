Do a comprehensive security review of our Supabase Postgres database. Here's the full context:

## Database Schema (10 tables)

**User-owned tables (per-user RLS via auth.uid() = user_id):**
- `profiles` — UUID id → auth.users. Has SELECT policy 'qual: true' (public read), INSERT 'with_check: auth.uid() = id', UPDATE 'qual: auth.uid() = id'
- `weapon_instance` — bigint id, user_id UUID → auth.users. SELECT/INSERT/UPDATE/DELETE all scoped to auth.uid() = user_id
- `player_inventory` — NEW table. bigint id, user_id, slot_index (0-19 CHECK), weapon_instance_id FK. SELECT/INSERT/UPDATE/DELETE all scoped to auth.uid() = user_id. UNIQUE on (user_id, slot_index) and (user_id, weapon_instance_id)

**Reference/static data tables (authenticated read, service_role write):**
- `weapon_template` — SELECT: auth.role() = 'authenticated', ALL: auth.role() = 'service_role'
- `attack` — same pattern
- `weapon_template_attack_mapping` — same pattern
- `monster_template` — SELECT: auth.role() = 'authenticated', ALL: auth.role() = 'service_role'  (NEWLY SECURED — RLS was previously disabled)
- `monster_template_attack_mapping` — same (NEWLY SECURED)
- `monster_instance` — SELECT: auth.role() = 'authenticated', ALL: auth.role() = 'service_role'  (NEWLY SECURED)

**Restricted table:**
- `invite_keys` — Deny all access to anon (qual: false, with_check: false). SELECT: auth.role() = 'authenticated'

## Privileges (post-fix, verified)
- anon: NO INSERT/UPDATE/DELETE on any table. SELECT only on: attack, weapon_template, weapon_template_attack_mapping, monster_instance, monster_template, monster_template_attack_mapping (all gated by RLS policies requiring authenticated role)
- anon: NO SELECT on: profiles, weapon_instance, player_inventory, invite_keys
- authenticated: SELECT/INSERT/UPDATE/DELETE on profiles, weapon_instance, player_inventory. SELECT on all static data tables.
- authenticated: ALSO has INSERT/UPDATE/DELETE at table level on static data tables (weapon_template, monster_template, etc.) — but RLS policies restrict writes to service_role only.

## Config (config.toml)
- jwt_expiry = 3600 (1 hour)
- enable_refresh_token_rotation = true
- enable_anonymous_sign_ins = false
- enable_signup = true (email)
- minimum_password_length = 8
- Auth email confirmations disabled (enable_confirmations = false)
- Email double_confirm_changes = true
- Google + GitHub OAuth enabled

## Key questions:
1. Is authenticated role having table-level INSERT/UPDATE/DELETE grants on static data tables a risk, even though RLS policies block writes to service_role only?
2. The invite-verify edge function has verify_jwt = false. Is the defense sufficient (Origin validation + unguessable keys + service_role-only DB access)?
3. profiles SELECT policy is qual=true — all authenticated users can see all profiles. Intentional or exploitable?
4. Any other issues?

Rate findings: 🔴 CRITICAL, 🟡 WARNING, 🟢 OK. Be thorough but concise.