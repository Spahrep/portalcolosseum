/*
 * NOTE: This file is NOT used in production.
 * ============================================
 * The actual environment configuration is served by the Vercel serverless
 * function at /api/env.js, which injects environment variables at runtime.
 *
 * This placeholder file exists for local development only. For local dev,
 * either set environment variables and run a local server, or temporarily
 * replace the placeholder values below:
 *
 *   window.ENV = window.ENV || {};
 *   window.ENV.SUPABASE_URL = 'https://your-project.supabase.co';
 *   window.ENV.SUPABASE_ANON_KEY = 'sb_publishable_...';
 *
 * In production, /api/env.js handles this dynamically. See:
 *   api/env.js
 *   docs/setup-environment-variables.md
 */
